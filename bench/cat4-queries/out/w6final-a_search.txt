QUERY COUNT: 53
TOTAL DB TIME (parse+bind+exec): 45.56 ms

--- all queries, slowest first (total ms | exec ms) ---
    4.08 |     3.80 | SELECT id, workspace_id, document_type, title, parent_id, position, ticket_number, properties, created_at, updated_at, created_by, visibility FROM documents WHERE workspace_id = $1 AND archived_at IS NULL AND deleted_at 
    3.74 |     0.36 | SELECT DISTINCT ON (project_id) project_id, project_name FROM ( -- Explicit sprint assignee_ids SELECT (s.properties->>'project_id')::uuid as project_id, proj.title as project_name FROM documents s JOIN documents proj ON
    3.72 |     2.08 | SELECT da.document_id, da.related_id as id, da.relationship_type as type, d.title, d.properties->>'color' as color FROM document_associations da LEFT JOIN documents d ON da.related_id = d.id WHERE da.document_id = ANY($1
    3.26 |     1.73 | SELECT d.id, d.title, d.properties, prog_da.related_id as program_id, d.archived_at, d.created_at, d.updated_at, d.converted_from_id, (d.properties->>'owner_id')::uuid as owner_id, u.name as owner_name, u.email as owner_
    2.60 |     2.00 | SELECT d.id, d.title, d.properties, d.ticket_number, NULL as content, d.created_at, d.updated_at, d.created_by, d.started_at, d.completed_at, d.cancelled_at, d.reopened_at, d.converted_from_id, u.name as assignee_name, C
    2.58 |     0.21 | SELECT DISTINCT ON (project_id) project_id, project_name FROM ( -- Explicit sprint assignee_ids SELECT (s.properties->>'project_id')::uuid as project_id, proj.title as project_name FROM documents s JOIN documents proj ON
    2.35 |     0.94 | SELECT d.id, d.title, d.properties, d.archived_at, d.created_at, d.updated_at, COALESCE((d.properties->>'owner_id')::uuid, d.created_by) as owner_id, u.name as owner_name, u.email as owner_email, (SELECT COUNT(*) FROM do
    1.70 |     0.17 | SELECT DISTINCT proj.id as project_id, proj.title as project_title, prog.title as program_name FROM documents s JOIN documents proj ON (s.properties->>'project_id')::uuid = proj.id AND proj.document_type = 'project' LEFT
    1.12 |     0.31 | SELECT s.id, s.title, s.properties, da.related_id as project_id FROM documents s LEFT JOIN document_associations da ON da.document_id = s.id AND da.relationship_type = 'project' WHERE s.workspace_id = $1 AND s.document_t
    1.10 |     0.56 | SELECT af.id, af.detector, af.severity, af.status, af.title, af.body, af.evidence, af.proposed_action, af.document_id, af.project_id, af.rule_based_only, af.created_at, d.title AS document_title, d.ticket_number FROM age
    0.98 |     0.61 | SELECT id, title, document_type, visibility FROM documents WHERE workspace_id = $1 AND document_type IN ('wiki', 'issue', 'project', 'program') AND deleted_at IS NULL AND title ILIKE $2 AND (visibility = 'workspace' OR c
    0.95 |     0.08 | SELECT DISTINCT s.id as sprint_id FROM documents i JOIN document_associations da ON da.document_id = i.id AND da.relationship_type = 'sprint' JOIN documents s ON s.id = da.related_id AND s.document_type = 'sprint' WHERE 
    0.94 |     0.10 | SELECT da.related_id as sprint_id, COUNT(*) as count FROM document_associations da JOIN documents d ON d.id = da.document_id WHERE da.related_id = ANY($1) AND da.relationship_type = 'sprint' AND d.document_type = 'issue'
    0.84 |     0.07 | SELECT s.id as sprint_id, (s.properties->>'sprint_number')::int as sprint_number, s.properties->'plan_approval' as plan_approval, s.properties->'review_approval' as review_approval, s.title as sprint_title, da.related_id
    0.78 |     0.50 | SELECT id, workspace_id, document_type, title, parent_id, position, ticket_number, properties, created_at, updated_at, created_by, visibility FROM documents WHERE workspace_id = $1 AND archived_at IS NULL AND deleted_at 
    0.76 |     0.13 | SELECT id, content FROM documents WHERE workspace_id = $1 AND document_type = 'weekly_plan' AND (properties->>'person_id') = $2 AND (properties->>'week_number')::int = $3 AND archived_at IS NULL AND deleted_at IS NULL
    0.70 |     0.07 | SELECT d.id::text as id, d.title as name, 'person' as document_type FROM documents d WHERE d.workspace_id = $1 AND d.document_type = 'person' AND d.archived_at IS NULL AND d.deleted_at IS NULL AND d.title ILIKE $2 ORDER 
    0.67 |     0.10 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.65 |     0.11 | SELECT id, title FROM documents WHERE workspace_id = $1 AND document_type = 'person' AND (properties->>'user_id') = $2 LIMIT 1
    0.59 |     0.08 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.54 |     0.08 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.53 |     0.10 | SELECT id, title, content, properties, created_at, updated_at FROM documents WHERE workspace_id = $1 AND document_type = 'weekly_retro' AND (properties->>'person_id') = $2 AND (properties->>'week_number')::int = $3 AND a
    0.53 |     0.09 | SELECT id, title, content, properties, created_at, updated_at FROM documents WHERE workspace_id = $1 AND document_type = 'weekly_plan' AND (properties->>'person_id') = $2 AND (properties->>'week_number')::int = $3 AND ar
    0.51 |     0.10 | SELECT d.id, d.properties->>'user_id' as user_id, d.title as name, COALESCE(d.properties->>'email', u.email) as email, CASE WHEN d.archived_at IS NOT NULL THEN true ELSE false END as "isArchived", CASE WHEN d.properties-
    0.49 |     0.09 | SELECT id, title, properties FROM documents WHERE workspace_id = $1 AND document_type = 'weekly_retro' AND (properties->>'person_id') = $2 AND (properties->>'week_number')::int = $3 AND archived_at IS NULL AND deleted_at
    0.49 |     0.08 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.49 |     0.08 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.48 |     0.07 | SELECT id, title, properties, created_at, updated_at FROM documents WHERE workspace_id = $1 AND document_type = 'standup' AND (properties->>'author_id') = $2 AND (properties->>'date') = ANY($3) AND deleted_at IS NULL ORD
    0.47 |     0.08 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.45 |     0.05 | SELECT w.id, w.name, wm.role FROM workspaces w JOIN workspace_memberships wm ON w.id = wm.workspace_id WHERE wm.user_id = $1 AND w.archived_at IS NULL ORDER BY w.name
    0.44 |     0.08 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.43 |     0.07 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.43 |     0.07 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.42 |     0.06 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.40 |     0.06 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.40 |     0.07 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.39 |     0.06 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.37 |     0.05 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.33 |     0.08 | SELECT id FROM documents WHERE workspace_id = $1 AND document_type = 'person' AND (properties->>'user_id')::uuid = $2
    0.33 |     0.04 | SELECT w.id, w.name, wm.role FROM workspaces w LEFT JOIN workspace_memberships wm ON w.id = wm.workspace_id AND wm.user_id = $2 WHERE w.id = $1
    0.22 |     0.07 | SELECT id, created_at, expires_at, last_activity FROM sessions WHERE id = $1
    0.22 |     0.04 | SELECT role FROM workspace_memberships WHERE workspace_id = $1 AND user_id = $2
    0.21 |     0.03 | SELECT id, email, name, is_super_admin FROM users WHERE id = $1
    0.21 |     0.03 | SELECT sprint_start_date FROM workspaces WHERE id = $1
    0.20 |     0.03 | SELECT sprint_start_date FROM workspaces WHERE id = $1
    0.20 |     0.04 | SELECT id, created_at, expires_at, last_activity FROM sessions WHERE id = $1
    0.20 |     0.03 | SELECT role FROM workspace_memberships WHERE workspace_id = $1 AND user_id = $2
    0.19 |     0.03 | SELECT role FROM workspace_memberships WHERE workspace_id = $1 AND user_id = $2
    0.18 |     0.03 | SELECT role FROM workspace_memberships WHERE workspace_id = $1 AND user_id = $2
    0.18 |     0.03 | SELECT role FROM workspace_memberships WHERE workspace_id = $1 AND user_id = $2
    0.17 |     0.02 | SELECT role FROM workspace_memberships WHERE workspace_id = $1 AND user_id = $2
    0.17 |     0.02 | SELECT role FROM workspace_memberships WHERE workspace_id = $1 AND user_id = $2
    0.17 |     0.02 | SELECT sprint_start_date FROM workspaces WHERE id = $1

--- duplicate shapes (N+1 candidates) ---
  x14  SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
  x7  SELECT role FROM workspace_memberships WHERE workspace_id = $1 AND user_id = $2
  x3  SELECT sprint_start_date FROM workspaces WHERE id = $1
  x2  SELECT id, created_at, expires_at, last_activity FROM sessions WHERE id = $1
  x2  SELECT DISTINCT ON (project_id) project_id, project_name FROM ( -- Explicit sprint assignee_ids SELECT (s.properties->>'project_id')::uuid as project_id, proj.title as project_name FROM documents s JO

QUERY COUNT: 45
TOTAL DB TIME (parse+bind+exec): 36.39 ms

--- all queries, slowest first (total ms | exec ms) ---
    4.20 |     2.65 | SELECT da.document_id, da.related_id as id, da.relationship_type as type, d.title, d.properties->>'color' as color FROM document_associations da LEFT JOIN documents d ON da.related_id = d.id WHERE da.document_id = ANY($1
    2.54 |     0.15 | SELECT DISTINCT ON (project_id) project_id, project_name FROM ( -- Explicit sprint assignee_ids SELECT (s.properties->>'project_id')::uuid as project_id, proj.title as project_name FROM documents s JOIN documents proj ON
    2.41 |     1.08 | SELECT d.id, d.title, d.properties, d.archived_at, d.created_at, d.updated_at, COALESCE((d.properties->>'owner_id')::uuid, d.created_by) as owner_id, u.name as owner_name, u.email as owner_email, (SELECT COUNT(*) FROM do
    2.34 |     1.54 | SELECT af.id, af.detector, af.severity, af.status, af.title, af.body, af.evidence, af.proposed_action, af.document_id, af.project_id, af.rule_based_only, af.created_at, d.title AS document_title, d.ticket_number FROM age
    2.15 |     1.68 | SELECT d.id, d.title, d.properties, d.ticket_number, NULL as content, d.created_at, d.updated_at, d.created_by, d.started_at, d.completed_at, d.cancelled_at, d.reopened_at, d.converted_from_id, u.name as assignee_name, C
    1.83 |     0.77 | SELECT d.id, d.title, d.properties, prog_da.related_id as program_id, d.archived_at, d.created_at, d.updated_at, d.converted_from_id, (d.properties->>'owner_id')::uuid as owner_id, u.name as owner_name, u.email as owner_
    1.44 |     0.20 | UPDATE webhook_deliveries d SET status = 'delivering', updated_at = now() WHERE d.id IN ( SELECT c.id FROM webhook_deliveries c JOIN webhook_subscriptions s ON s.id = c.subscription_id WHERE c.status IN ('pending', 'fail
    1.33 |     0.13 | SELECT DISTINCT s.id as sprint_id FROM documents i JOIN document_associations da ON da.document_id = i.id AND da.relationship_type = 'sprint' JOIN documents s ON s.id = da.related_id AND s.document_type = 'sprint' WHERE 
    1.17 |     0.10 | SELECT DISTINCT ON (project_id) project_id, project_name FROM ( -- Explicit sprint assignee_ids SELECT (s.properties->>'project_id')::uuid as project_id, proj.title as project_name FROM documents s JOIN documents proj ON
    1.15 |     0.15 | UPDATE webhook_deliveries d SET status = 'delivering', updated_at = now() WHERE d.id IN ( SELECT c.id FROM webhook_deliveries c JOIN webhook_subscriptions s ON s.id = c.subscription_id WHERE c.status IN ('pending', 'fail
    1.10 |     0.14 | UPDATE webhook_deliveries d SET status = 'delivering', updated_at = now() WHERE d.id IN ( SELECT c.id FROM webhook_deliveries c JOIN webhook_subscriptions s ON s.id = c.subscription_id WHERE c.status IN ('pending', 'fail
    0.90 |     0.23 | SELECT s.id, s.title, s.properties, da.related_id as project_id FROM documents s LEFT JOIN document_associations da ON da.document_id = s.id AND da.relationship_type = 'project' WHERE s.workspace_id = $1 AND s.document_t
    0.85 |     0.56 | SELECT id, workspace_id, document_type, title, parent_id, position, ticket_number, properties, created_at, updated_at, created_by, visibility FROM documents WHERE workspace_id = $1 AND archived_at IS NULL AND deleted_at 
    0.82 |     0.06 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.80 |     0.13 | SELECT d.id, d.properties->>'user_id' as user_id, d.title as name, COALESCE(d.properties->>'email', u.email) as email, CASE WHEN d.archived_at IS NOT NULL THEN true ELSE false END as "isArchived", CASE WHEN d.properties-
    0.73 |     0.13 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.70 |     0.10 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.69 |     0.12 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.66 |     0.08 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.66 |     0.03 | SELECT da.related_id as sprint_id, COUNT(*) as count FROM document_associations da JOIN documents d ON d.id = da.document_id WHERE da.related_id = ANY($1) AND da.relationship_type = 'sprint' AND d.document_type = 'issue'
    0.65 |     0.09 | UPDATE webhook_deliveries d SET status = 'delivering', updated_at = now() WHERE d.id IN ( SELECT c.id FROM webhook_deliveries c JOIN webhook_subscriptions s ON s.id = c.subscription_id WHERE c.status IN ('pending', 'fail
    0.56 |     0.12 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.53 |     0.06 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.52 |     0.08 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.51 |     0.08 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.47 |     0.09 | SELECT d.id, d.properties->>'user_id' as user_id, d.title as name, COALESCE(d.properties->>'email', u.email) as email, CASE WHEN d.archived_at IS NOT NULL THEN true ELSE false END as "isArchived", CASE WHEN d.properties-
    0.44 |     0.04 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.36 |     0.06 | SELECT id FROM documents WHERE workspace_id = $1 AND document_type = 'person' AND (properties->>'user_id')::uuid = $2
    0.35 |     0.03 | SELECT s.id as sprint_id, (s.properties->>'sprint_number')::int as sprint_number, s.properties->'plan_approval' as plan_approval, s.properties->'review_approval' as review_approval, s.title as sprint_title, da.related_id
    0.35 |     0.05 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.34 |     0.05 | SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
    0.32 |     0.06 | SELECT id, created_at, expires_at, last_activity FROM sessions WHERE id = $1
    0.28 |     0.03 | SELECT role FROM workspace_memberships WHERE workspace_id = $1 AND user_id = $2
    0.27 |     0.04 | SELECT role FROM workspace_memberships WHERE workspace_id = $1 AND user_id = $2
    0.24 |     0.03 | SELECT id, email, name, is_super_admin FROM users WHERE id = $1
    0.23 |     0.03 | SELECT role FROM workspace_memberships WHERE workspace_id = $1 AND user_id = $2
    0.23 |     0.04 | SELECT role FROM workspace_memberships WHERE workspace_id = $1 AND user_id = $2
    0.21 |     0.03 | SELECT role FROM workspace_memberships WHERE workspace_id = $1 AND user_id = $2
    0.20 |     0.06 | SELECT id, content FROM documents WHERE workspace_id = $1 AND document_type = 'weekly_plan' AND (properties->>'person_id') = $2 AND (properties->>'week_number')::int = $3 AND archived_at IS NULL AND deleted_at IS NULL
    0.17 |     0.02 | SELECT w.id, w.name, wm.role FROM workspaces w JOIN workspace_memberships wm ON w.id = wm.workspace_id WHERE wm.user_id = $1 AND w.archived_at IS NULL ORDER BY w.name
    0.17 |     0.02 | SELECT role FROM workspace_memberships WHERE workspace_id = $1 AND user_id = $2
    0.15 |     0.04 | SELECT sprint_start_date FROM workspaces WHERE id = $1
    0.13 |     0.01 | SELECT w.id, w.name, wm.role FROM workspaces w LEFT JOIN workspace_memberships wm ON w.id = wm.workspace_id AND wm.user_id = $2 WHERE w.id = $1
    0.12 |     0.02 | SELECT sprint_start_date FROM workspaces WHERE id = $1
    0.10 |     0.02 | SELECT id, created_at, expires_at, last_activity FROM sessions WHERE id = $1

--- duplicate shapes (N+1 candidates) ---
  x12  SELECT s.id, s.user_id, s.workspace_id, s.expires_at, s.last_activity, s.created_at, u.is_super_admin FROM sessions s JOIN users u ON s.user_id = u.id WHERE s.id = $1
  x6  SELECT role FROM workspace_memberships WHERE workspace_id = $1 AND user_id = $2
  x4  UPDATE webhook_deliveries d SET status = 'delivering', updated_at = now() WHERE d.id IN ( SELECT c.id FROM webhook_deliveries c JOIN webhook_subscriptions s ON s.id = c.subscription_id WHERE c.status 
  x2  SELECT id, created_at, expires_at, last_activity FROM sessions WHERE id = $1
  x2  SELECT d.id, d.properties->>'user_id' as user_id, d.title as name, COALESCE(d.properties->>'email', u.email) as email, CASE WHEN d.archived_at IS NOT NULL THEN true ELSE false END as "isArchived", CAS
  x2  SELECT sprint_start_date FROM workspaces WHERE id = $1
  x2  SELECT DISTINCT ON (project_id) project_id, project_name FROM ( -- Explicit sprint assignee_ids SELECT (s.properties->>'project_id')::uuid as project_id, proj.title as project_name FROM documents s JO

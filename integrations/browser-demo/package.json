{
  "name": "@ship/browser-demo",
  "version": "0.1.0",
  "description": "Authorization Code + PKCE in a single-page app that lists the user's documents. Speaks to Ship only through @ship/sdk.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@ship/sdk": "workspace:*"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vite": "6.4.1"
  }
}
